// Configurazione Tutor AI
// Per usare una vera AI inserisci qui l'URL della Firebase Function dopo il deploy.
// Finché endpoint è vuoto, l'app usa il Tutor locale di sicurezza, senza costi API.
const TUTOR_SETTINGS = {
  enabled: true,
  endpoint: "",
  demoLocalTutor: true
};
